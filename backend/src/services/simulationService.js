/**
 * simulationService.js
 * Orquestra o encerramento de uma rodada e o cálculo financeiro de todas as lojas.
 * Implementação completa no B-13.
 */

async function processRound(roundId) {
  // TODO (B-13): buscar todos os RoundConfigs da rodada,
  // calcular DRE via financeService para cada loja,
  // persistir FinancialResult e atualizar estoque.
  console.log(`[simulationService] processRound chamado para roundId=${roundId} (pendente B-13)`);
}

module.exports = { processRound };
