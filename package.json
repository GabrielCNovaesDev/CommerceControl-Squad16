{
  "name": "commercecontrol-squad16",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"BACK,FRONT\" --prefix-colors \"cyan,magenta\" \"npm run dev:local --prefix backend\" \"npm run dev --prefix frontend\"",
    "setup": "npm install --prefix backend --legacy-peer-deps && npm install --prefix frontend && npm run setup:local --prefix backend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
